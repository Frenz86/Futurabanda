pgzero==1.2.1
pygame==2.6.1
SpeechRecognition==3.17.0
PyAudio==0.2.14
faster-whisper==1.1.0

# Al primo avvio faster-whisper scarica da solo il modello "small" (italiano
# incluso, multilingua) da Hugging Face e lo mette in cache in
# ~/.cache/huggingface; da li' in poi funziona offline. Nessun download
# manuale richiesto (a differenza del vecchio modello Vosk).
